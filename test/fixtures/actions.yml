one:
  action_type: 'like'
  user: victor (User)
  target: one (Post)

two:
  action_type: 'like'
  user: victor (User)
  target: one (Comment)

three:
  action_type: 'favor'
  user: victor (User)
  target: one (Post)
